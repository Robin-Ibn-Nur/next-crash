import React from 'react';

const About = () => {
    throw new Error("This is an error")
    return (
        <div>
            this is about
        </div>
    );
};

export default About;